@model SGE.Models.Profesor

@{
    ViewData["Title"] = "Details";
}

<h1>Ver detalles</h1>

<div>
    <h4>Profesor</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.ProfesorId" class="btn btn-info">Modificar</a> |
    <a asp-action="Index" class="btn btn-light">Regresar</a>
</div>
